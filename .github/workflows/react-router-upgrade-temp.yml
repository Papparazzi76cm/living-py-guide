name: Validate React Router 7 upgrade

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  validate-router-upgrade:
    if: github.head_ref == 'agent/react-router-7'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Refresh lockfile for React Router 7
        run: npm install --package-lock-only

      - name: Install upgraded dependency tree
        run: npm ci

      - name: Runtime security audit
        run: npm audit --omit=dev --audit-level=moderate

      - name: Lint
        run: npm run lint

      - name: Production build
        run: npm run build

      - name: Persist lockfile
        shell: bash
        run: |
          if git diff --quiet -- package-lock.json; then
            echo 'No lockfile changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package-lock.json
          git commit -m 'Refresh lockfile for React Router 7'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
